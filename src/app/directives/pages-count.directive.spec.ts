import { PagesCountDirective } from './pages-count.directive';

describe('PagesCountDirective', () => {
  it('should create an instance', () => {
    const directive = new PagesCountDirective();
    expect(directive).toBeTruthy();
  });
});
